<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SNOTES" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">@color/TEXTCOLOR</item>
        <item name="rippleColor">@color/greydisabled</item>
    </style>

    <style name="Theme.SNOTES" parent="Base.Theme.SNOTES" />
    <style name="Textlayoutstyle" parent="ThemeOverlay.Material3.AutoCompleteTextView.OutlinedBox">
        <item name="android:layout_marginHorizontal">20dp</item>
        <item name="boxBackgroundColor">@color/grey</item>
        <item name="boxStrokeColor">@color/blacklite</item>
        <item name="boxCornerRadiusBottomStart">24dp</item>
        <item name="boxCornerRadiusBottomEnd">24dp</item>
        <item name="boxCornerRadiusTopEnd">24dp</item>
        <item name="boxCornerRadiusTopStart">24dp</item>
    </style>
</resources>